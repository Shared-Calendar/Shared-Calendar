<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.sharedcalendar.mapper.UserMapper">
    <insert id="createUser" >
        INSERT INTO user(
            user_id,
            password,
            email,
            invite_url,
            activate,
            created_at,
            updated_at
        )values (
                 #{userId},
                 #{password},
                 #{email},
                 1,
                 1,
                 NOW(),
                 NOW()
                        );
    </insert>

</mapper>