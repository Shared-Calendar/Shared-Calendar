<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.sharedcalendar.mapper.UserMapper">

    <select id="getUser" resultType="study.sharedcalendar.dto.UserDTO">
        SELECT * FROM users WHERE user_id = #{userId};
    </select>

    <insert id="createUser">
        INSERT INTO users(
            user_id,
            password,
            email,
            invite_url,
            activate,
            created_at,
            updated_at,
            login_count
        )values (
            #{userId},
            #{password},
            #{email},
            #{inviteUrl},
            1,
            NOW(),
            NOW(),
            0
        );
    </insert>

</mapper>